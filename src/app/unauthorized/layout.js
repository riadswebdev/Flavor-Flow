export const metadata = {
  title: "FlavorFlow | Unauthorized Access (401)",
  description:
    "You do not have permission to access this restricted page on FlavorFlow.",
};

export default function UnauthorizedLayout({ children }) {
  return <>{children}</>;
}
